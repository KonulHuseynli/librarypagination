@using UniversityProject.Models
@model RelatedProducts
<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
             <div class="col-md-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  
                  <p class="card-description">
                   Delete Related Products
                  </p>
                  
                   <span>TITLE:</span>
                   <span>@Model.Title</span>

                   <br/>
                       <br/>
                   <span>Writer:</span>
                   <span>@Model.Writer</span>

                        <br/>
                            <br/>

                  <span>FilePath:</span>
                   <span>@Model.FilePath</span>

                   <form method="post" asp-action="deletecomponent" asp-controller="relatedproduct" asp-area="admin" asp-route-id="@Model.Id">
                      <a class="btn btn-primary" asp-action="index" asp-controller="relatedproduct" asp-area="admin"> Cancel</a>
                   </form>
                </div>
              </div>
            </div>
        </div>
    </div>
</div>
